{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Produits creation</h1>
	
   {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('adminProduits_index') }}">
            Retour
        </a>
    </li>
</ul>
{% endblock %}
